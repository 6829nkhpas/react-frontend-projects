import React from 'react'

function inputBox({
    label,
    amount,
    onAmountChange,
    onCurrencyChange,
    currencyOptions = [],
    selectCurrency = "usd",
    amountDisable = false,
    currencyDisable = false,
}) {
  return (
    <div>inputBox</div>
  )
}

export default inputBox